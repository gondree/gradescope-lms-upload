# gradescope-lms-upload
Scripts to takes Gradescope annotated PDFs and put them in bulk-upload-feedbackfile format for Moodle and Canvas

## Dependencies
Python 3.X and

    pip install pyyaml
    pip install canvasapi